import type { Content } from "./types";
import { en } from "./en";

/**
 * PLACEHOLDER — replaced with natively-written Italian copy in Milestone 5.
 * Until then the IT route falls back to English so it never renders empty.
 */
export const it: Content = en;
